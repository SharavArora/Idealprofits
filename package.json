{
  "name": "idealprofits-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run install-deps && react-scripts build",
    "install-deps": "npm install --legacy-peer-deps",
    "prebuild": "npm run install-deps"
  }
}
